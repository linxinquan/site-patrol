# 蓝图落地 · 用户操作流程（驻场设计代表）

> **面向谁**：现场驻场设计代表 —— 当前唯一真实用户。也供项目负责人与交付带教使用。
> **用途**：照着做就能完成一天的现场工作。每一步都写清「点哪里 → 看到什么 → 注意什么」。
> **与规格的分工**：`docs/specs/CAPTURE_FLOW.md` / `DEFECTS_FLOW.md` 讲**系统怎么实现**（含偏离清单与数据契约），本文只讲**人怎么操作**。两者冲突时以规格与 `FEATURE_INVENTORY.md` 为准。
> **状态口径**：✅ 可用 ｜ 🟡 有前提或受限 ｜ 🔴 暂不可用 ｜ ⛔ 已冻结/下线。核实日期 2026-09-18，与 `FEATURE_INVENTORY.md` 对齐。

---

## 0 先看这一张图：一次完整闭环

```
① 巡场/随手拍          ② 现场拍照            ③ AI 识别
   选图纸部位      →      相机+水印      →     手动点「AI 分析」
                                                    │
                                                  ④ 量尺校对 + 问题描述
                                                    │
                                                  ⑤ 保存记录
                                                    │
              ┌─────────────────────────────────────┘
              ▼
        ⑥ 验收记录工作台 ──「转入问题清单」──→ ⑦ 问题清单
                                                    │
                        设计师处置 ←───────────────┤
                        施工方回复销项 ←───────────┤
                                                    ▼
                                              ⑧ 导出报告（HTML/PDF/DOCX/XLSX）
```

**你每天真正要做的只有 5 个动作**：巡场 → 拍 → 让 AI 认 → 写描述（手打或说）→ 保存。
后面的事（转入清单、断责、销项、出报告）都在**办公室里坐着点**，不用在现场做。

---

## 1 开始之前

### 1.1 登录（当前是演示态）

1. 打开 App → 开屏登录页 → 点**「开始使用」**即进入 🔴
   （写死 demo 用户，**没有账号/密码/服务端校验**）
2. 首次进入走两步引导：**选身份**（4 个 mock 用户）→ **选项目**
3. 想换身份 / 换项目 / 退出：右上角头像 → 个人中心侧边栏

> 演示时遇到"为什么不用输密码"，直接说明这是演示态即可，别现场圆。

### 1.2 界面地图

**底部 5 个 tab（纯文字）**：`项目` · `图纸` · `验收` · `巡场` · `问题`

**首页 5 个快捷入口**：`工地巡场` · `拍照验收` · `AR量尺` · `验收记录` · `巡场报告`

另：首页「今日待办」卡片 →「查看全部」也进问题清单。

### 1.3 数据是按项目隔离的

所有记录、巡场路线、判定门槛都挂在**当前项目**下。切了项目就像换了台机器 —— 看不到上个项目的数据。**开始干活前先确认顶部项目名对不对。**

---

## 2 主线一：拍照验收（现场取证）

> 入口：底部「验收」tab 或首页「拍照验收」
> 概念三步：**选图纸部位 → 现场拍照 → 识别并保存** ✅

### 2.1 第 1 步 · 选图纸与部位

- 进入后自动带出当前项目的第一张图纸（从图纸 tab 进来会带上你刚才看的那张）
- 在图上**点选问题位置**：系统会夹取到图内 0.02~0.98，并**自动吸附到最近的锚点**（阈值 0.12）
- 选中点会换算成**图纸世界坐标（mm）**，写进水印 —— 这是后面能"按图纸位置定位问题"的关键
- 点错了：点**「重选部位」**
- 📌 越靠近锚点（轴网交点、墙角）吸附越准。**选点时尽量点在锚点附近**，别点在大片空白房间中央

### 2.2 第 2 步 · 现场拍照

- 进入拍照页会**自动拉起相机**（移动端）
- 移动端：真实相机 + 权限引导 + 相册兜底；Web：走相册选图
- 拍完点**「使用」**确认。系统随即自动做三件事：**压缩 → 烧录防篡改水印 → 清空上一次识别结果**
- 水印内容：信息栏 + 全图斜水印 + **图纸坐标** + **凭证号** + **哈希**（SHA256/MD5）✅
- ⚠️ 拍照后会有 **0.3~0.8 秒卡顿**（水印在主线程烧录），不是卡死，别重复点
- 🔴 **水印里的 GPS 目前来自你在"附近定位"里手选的 mock 点位，不是设备真实定位** —— 演示时可提，别声称是自动定位

### 2.3 第 3 步 · AI 识别（**必须手动点**）

- 拍完**不会自动识别**，要点结果区里的**「AI 分析」** 🟡
- 800ms 扫描动画后出结果，结果区分 4 段：**① 缺陷识别 ② 量尺校对 ③ 问题描述 ④ 拍照记录**
- 模型：`qwen3.8-max`，走 `POST /api/vision`
- 🟡 **Web 端走的是预设 mock，要真实识别请用真机**
- 🟡 **断网即失效** —— 现场没信号就先拍照保存，回头补描述

### 2.4 第 4 步 · 量尺校对（可选，但建议做）

- 拿**图纸侧尺寸** vs **照片侧实测**做校对，双容差默认 **±10mm / ±5%**，可当场改
- 依赖图纸已做坐标校准（mm/px）；没校准就只有图纸侧的数字
- 逐项判定：**合格 / 超差 / 需复核**

### 2.5 第 5 步 · 写问题描述

- 手打，或点**语音按钮**说话追加（设备端离线中文 ASR）✅
- 建议写全三件事：**部位 + 现象 + 尺寸偏差**。例：
  > 7 层 3 轴交 C 轴卫生间，地漏周边防水收口开裂约 120mm，实测低于成活面 8mm

### 2.6 第 6 步 · 保存

- 点**「保存记录」**。按钮点不动时看文案就知道缺什么：
  | 按钮文案 | 含义 |
  |---|---|
  | 请先拍照 | 还没拍 |
  | 请确认使用照片 | 拍了但没点「使用」 |
  | 分析中… | AI 还在跑 |
  | 已保存（可继续拍摄） | 已存好 |
- 保存会同时做两件事：① 写一条**验收记录** ② 生成**1 条聚合缺陷**进问题清单
- 保存后两个出口：**「继续验收」**（接着拍下一处）｜**「查看验收记录」**
- 🟡 **Web 端照片存在内存里，刷新即丢**；移动端落盘

### 2.7 事后 · 验收记录工作台

- 入口：首页「验收记录」，或保存后点「查看验收记录」
- 有什么：统计条 + 筛选（**时间 / 楼层 / 仅 AI 识别**）+ 分组网格 + 卡片详情 + 批量操作 ✅
- **「转入问题清单」**：把验收记录里的 AI 缺陷条目**逐条**变成问题清单条目（支持单项或批量）
- 「删除」：只删验收记录本身

> ⚠️ **一个必须知道的坑**：保存时已经自动生成了 1 条聚合缺陷；再到工作台点「转入」会**再产生 N 条**。
> 同一次拍照可能留下 **1 + N 条**。**两条路选一条走**：要么保存即用、要么回去批量转入，别两个都做。

---

## 3 主线二：问题清单与闭环

> 入口：底部「问题」tab（也可从首页「今日待办 → 查看全部」进）

### 3.1 看板怎么读

- 状态分段：**全部 / 待整改 / 整改中 / 已销项 / 已拒绝**
- 卡片字段：部位 · 类型 · 严重度 · 责任人 · 责任单位 · 备注 · 处置 · 回复 ✅
- 严重度：红 / 橙 / 黄 / 绿；另有重要性四象限
- 🟡 **AI 目前不返回严重程度，一律默认"橙"** —— 需要区分严重度时请手工在描述里写明

> 🔴 **「整改中」「已拒绝」两个状态目前没有任何入口能写进去。**
> 界面上、首页待办、报告统计里都能见到它们，但你**无法把一条问题推进到这两个状态**（只有 mock 种子数据里才有）。
> 当前真实可走的通路只有一条：**待整改 →（销项）→ 已销项**。

### 3.2 设计师处置（你 / 专业负责人）

点卡片进详情页，三个动作：

| 动作 | 对状态的影响 | 说明 |
|---|---|---|
| **远程已解决** | 直接 → **已销项** | 说明**必填** |
| **远程已答复** | 状态不变，只记处置人/时间/说明 | 说明选填 |
| **需要到现场** | 状态不变 | 说明选填 |

- ⚠️ 处置后这张卡变**只读**、按钮区隐藏，**没有撤销 / 重处置入口**。点之前想清楚。
- 「远程已解决」等于**你替施工方关了这条** —— 只在照片和说明足以支撑结论时用。

### 3.3 施工方回复与销项

- 「填写回复」后两个出口：
  - **「仅保存，待复核」** → 状态不变（施工方说改好了，等你确认）
  - **「提交并销项」** → 已销项
- 记录里会留销项说明，报告里能区分是谁销的项：
  - `已完成（整改销项）`
  - `已完成（设计师远程销项）`

### 3.4 时间轴对比

- 同部位**多时点照片前后滑块对比** ✅（照片是真图）
- 用途：整改前后的同一角度对照，是给施工方看最有效的一张图
- 🟡 时间轴数据目前是 mock，只覆盖 1 个部位

### 3.5 AI 整改建议

- 优先用模型给出的建议，失败时用**本地 35 条建议库**兜底 ✅
- 定位是"起草话术"，不是结论 —— 发给施工方前自己过一遍

---

## 4 主线三：巡场

> 入口：底部「巡场」tab 或首页「工地巡场」

### 4.1 先有计划

- 每个项目 2 条种子路线（按 drawingKey 取图纸）
- 路线编辑器：**加点 / 拖动 / 删点 / 双击设为检查点 / 撤销 / 清空 / 保存** ✅
- 保存后巡场页自动刷新
- 📌 检查点就是"该到的地方"。**双击**才设为检查点，别漏

### 4.2 执行一次巡场

1. 点**「开始巡场」** → 沿规划路线走，页面有路线动画 + 实时统计条（**楼层 / 里程 / 点数 / 时长 / 模式**）
2. 中途要停 → **「暂停」**，再点**「继续」**
3. 走到检查点 → 打卡。**没走到就点会提示"尚未到达检查点，请沿路线继续前进"**
4. 发现问题 → 点**「标记问题」** → 直接跳到拍照验收（自动带上巡场来源）
   - 这条通路里保存按钮会变成**「保存巡场标记」**，保存后出口是**「返回巡场继续」/「继续标记下一个问题」** ✅
5. 走完点**「结束」**；完成后按钮变**「重新巡场」**

### 4.3 巡场现在还不准的几处（心里有数）

| 项 | 现状 |
|---|---|
| 打卡判定 | 🟡 按**动画进度**判定，不是 GPS 真实到位 |
| GPS 轨迹 | 🟡 只累计里程，**从不上图** |
| 穿墙检测 | 🟡 几何运算和红色高亮是真的，但**墙线资产只有 B05 一张（6 段）** |
| 巡检记录 `issueCount` | 🔴 恒为 0（标记问题后不回填） |
| 历史轨迹叠加 | 🔴 读写字段不一致 → 轨迹退化到原点 |
| 离线提示胶囊 | 🔴 写死文案 |

> 对外说巡场时，**说"路线引导 + 检查点打卡 + 随手标记问题"**，别说"GPS 轨迹回放"。

### 4.4 巡场报告归档

- 首页「巡场报告」→ 已生成报告的归档页，导出记录集中在这里回看 ✅
- 🟡 归档**只存元数据**，正文要重新导出

---

## 5 实测校核：两种量尺

### 5.1 照片量尺（图纸侧 + 照片侧）

1. 打开量尺页，确认当前图纸（**依赖 CAD 坐标校准**，没校准先做 §2.1 的选点/校准流程）
2. **图纸侧**：点两点直接量距 ✅
3. **照片侧标定**（三选一）：
   - **手动网格**：拉线建网格 + 单应透视校正（≥4 点）
   - **AI 自动识别模数网格** → 图上出绿标 → **你确认**
   - **锚物 / 参照物自动标定**（零输入；不够时回退两点比例法）
4. **AI 识别被测目标**：门窗宽高双线自动配对 → 自动生成工程编号（如 `M0921`）+ 模数吸附 → **逐条采纳** ✅
5. 判定：`误差 mm` + **项目级门槛**（容差 mm/% 和误差带门槛，按项目存，可配）✅
6. 结果：**合格 / 超差 / 需复核**，另有校对清单 + 偏差趋势（看有没有系统性偏差）✅

> ⚠️ AI 目标的误差带是**估算值**（标定残差 or 0.5%×尺寸），**不是真实重复采样**。
> 对外只能当"校核参考"，**不要当检定结论**。
> ⚠️ 所有 AI 能力**断网即失效**，会降级为手动标定。

### 5.2 AR 量尺（仅 iOS 真机）

1. 首页「AR量尺」（当前项目没有可用图纸会提示"无法使用AR量尺"）
2. 采点 A / B → 自动算距；长按清除；支持暂停 / 连续模式
3. **同一条边多采几次** → 自动取**中位数 ± 半极差**作为误差带，逐组判定 ✅
4. 距离门控：**>5m 丢弃**；**0.3~3m 是最佳区间**（有提示）✅
5. 系统偏差校正系数 k：偏差 **>15% 判定为测错** ✅
6. 批量保存（来源标记 `ar_lidar`，带误差带）

- ✅ 2026-09-18 已在 **iOS 真机实测通过**，可继续开发
- ⛔ Android AR **不做**（调研结论：不投入）
- 🔴 Web 只能看到硬编码演示值（如 2980mm），别在 Web 上演示 AR

---

## 6 交付：导出报告

> 入口：**问题清单页右上角**

1. 选**日期范围**
2. 手填**小结**
3. 选格式，4 张卡片：**HTML / PDF / DOCX / XLSX**
4. 拿文件：**Web 下载 / 移动端分享 / 桌面落盘** ✅

**报告章节**（空板块自动剔除）：照片墙 · 施工进度 · 台账 · 待协调问题 · 巡场清单闭环 · 量房记录 · 尺寸校对 · 巡场小结
**概览统计**含尺寸校对**合格 / 超差 / 需复核**计数。

> ⚠️ **两条口径必须记住：**
> 1. **报告只读问题清单（Defect），不读验收记录** —— 没「转入问题清单」的验收记录**不会出现在报告里**。导出前先去 §2.7 转一遍。
> 2. 报告归档**只存统计数字**，既答不了"这份报告包含哪些问题"，也**无法复现同一份报告**。要留底就自己存一份文件。

---

## 7 一天怎么用（建议节奏）

| 时段 | 做什么 |
|---|---|
| 到场前 | `项目` tab 确认项目/楼层对不对；`巡场` tab 看今天走哪条路线 |
| 现场巡场 | 「开始巡场」→ 发现问题「标记问题」→ 拍 → **先拍照再补 AI**（现场信号差） |
| 顺手发现 | 不在路线上的，走首页「拍照验收」，单独选部位拍 |
| 中午收工 | `验收` tab 进验收记录 → 筛「仅 AI 识别」→ **批量「转入问题清单」** |
| 下午 | `问题` tab 处置：远程已答复 / 远程已解决 / 需要到现场；施工方回复后确认销项 |
| 抽查 | 关键部位用量尺做一次实测校核，别全靠照片目测 |
| 收工前 | 问题清单右上角**导出报告** → 发群里 |

---

## 8 现在哪些还不能用（别在演示时翻车）

| 功能 | 现状 | 变通 |
|---|---|---|
| 真实登录 | 🔴 点「开始使用」即进，无账号密码 | 说明是演示态 |
| 添加更多账号 / 帮助 / 设置 | 🔴 只弹"敬请期待" | — |
| 整改中 / 已拒绝 | 🔴 无写入路径 | 只用「待整改 → 已销项」 |
| 数据同步到后端 | 🔴 后端只有 1 个接口 `/api/vision` | 全部走本机，**换机就没了** |
| Web 端照片 | 🟡 存内存，刷新即丢 | 用真机 |
| 导入 PDF / JPG / PNG 图纸 | 🔴 只弹提示，不解析 | — |
| 图层 / 布局面板 | 🔴 需真实 DWG 数据源 | — |
| DWG 上传 · 矢量看图（浩辰 OCF） | ⛔ 已冻结，待剥离到独立 Web 项目 | — |
| 量房成图 | ⛔ 首页入口已删，只剩孤儿路由 `/room-*` | 暂不使用 |
| 天气 · 语音记录页 · PDF 原稿 · 图层索引 | ⛔ 本轮已下线 | — |

---

## 9 平台能力矩阵（同一功能各端不一样）

| 能力 | iOS / Android | Web |
|---|---|---|
| 相机 | 真实相机 + 权限引导 | 相册选图 |
| AI 缺陷识别 | 真实模型 | mock 预设 |
| 照片存储 | 落盘 | 内存，**刷新即丢** |
| AR 量尺 | **仅 iOS 真机** | 硬编码演示值 |
| DWG 上传 / 矢量看图 | 冻结 | 可用（待剥离） |
| 报告产出方式 | 分享 | 下载 |

> 对外演示前**先确认用哪端**。AR 只能 iOS 真机，DWG 只能 Web —— 这两条最容易现场翻车。

---

## 10 常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 拍完看不到分析结果 | **要手动点「AI 分析」**，不会自动跑 |
| 保存按钮点不动 | 看按钮文案：请先拍照 / 请确认使用照片 / 分析中… |
| 拍照后卡半秒 | 水印在主线程烧录，属已知现象 |
| 问题清单里找不到"整改中" | 该状态目前写不进去，见 §3.1 |
| 报告里少了刚拍的问题 | 报告只读问题清单；先去验收记录「转入问题清单」 |
| 同一次拍照出现了多条问题 | 保存已生成 1 条聚合缺陷，「转入」会再生成 N 条，两条路只走一条 |
| 量尺判定标准前后不一致 | 量尺 / AR 走项目级门槛；**量房核尺仍写死 15mm/2%**，同一份报告里两种口径并存 |
| 换个手机数据全没了 | 现在全部是本地存储 + mock 种子，尚无同步 |
| 切了项目什么都看不到 | 正常，数据按项目隔离 |
| 判定结果能直接当结论发出去吗 | **不能**。AI 误差带是估算值，只作校核参考 |

---

## 11 待补充（本人补充区）

> 以下是我写不出来的现场经验，留给你补 —— 补完这份文档才真正能用于带教。

- [ ] **责任判定话术**：现场怎么跟施工方说"这是设计问题还是施工问题"？责任划分口径未成文，也是最容易被现场问倒的一条
- [ ] **每个问题拍几张、怎么拍**：要不要"远景定位 + 特写现象"两张？角度要求？照片规范
- [ ] **描述模板**：不同类型的固定写法（部位 + 现象 + 尺寸 + 影响）
- [ ] **判定门槛怎么定**：项目级容差 mm/% 实际取多少（谁拍板、依据哪本规范）
- [ ] **带教时最容易漏的一步**：新人第一次独立巡场，最常错在哪
- [ ] **报告对外发送前必须谁签**：设计代表 / 专业负责人 / 项目负责人？签收状态现在系统里没有
- [ ] **量化目标**：院里对驻场的期望（每周多少条问题算合格）

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 首版。按驻场设计代表视角，覆盖登录 → 拍照验收 → 问题清单闭环 → 巡场 → 量尺 → 报告全链路，并标注 11 处当前不可用/受限项。 |

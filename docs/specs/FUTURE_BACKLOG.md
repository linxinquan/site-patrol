# 未来功能清单（FUTURE BACKLOG）

> **用途**：收集「**决定要做但不放在本次**」的功能与能力。与 `DEVIATION_REGISTER.md` 的分工：
> - 本文件 = **尚未排期的功能**（有意识地推后，不是遗漏）；
> - `DEVIATION_REGISTER.md` = **已确认要修的偏离/缺陷**（含批次与决策）。
> **维护规则**：新增一条写「来源决策 + 触发条件 + 涉及范围」；被排期后从本表移除并落到 register。
> **基线**：2026-09-18

---

| # | 未来功能 | 来源 | 触发条件 | 涉及范围 / 前置 |
|---|---|---|---|---|
| **F1** | **水印真哈希校验**：把 `photoHash`（照片 SHA-256）与 `watermarkSerial`（凭证号）落库，打开记录详情时比对并显示"已校验/校验失败" | DV-01 选 B（本次只改文案，不再宣称"已效验"） | 需要对甲方/监理证明"照片未被篡改"时 | 需在 `Defect` 重新加回两个字段（2026-09-18 已按"无人使用"删除）+ 拍照时写入 + 详情页比对 UI + 服务端校验接口 |
| **F2** | **定位点识别真实坐标**：附近定位点不再用预置列表，改为按真实 GPS 反查地点；水印「项目」字段改用**真实项目名**（当前取定位点名称 `SiteLocation.name`） | DV-26 | 现场定位点与项目对不上、或需精确到坐标时 | `siteLocations` 数据源 + 水印 `WatermarkMeta.project` 语义 + 定位选择 UI |
| **F3** | **处置与回复页页内直接处置 / 回复**：现在该页只读，所有操作都要再进详情页 | DV-23 选 B（本次先降难度，维持只读） | 处置量大、需要连续处理时 | `DisposalReplyPage` 内嵌处置/回复表单（复用详情页的 `_DesignerActionSheet` / `_ReplyActionSheet`） |
| **F4** | **报告字段的人工录入入口**：`importance`（重要等级）、`building`（楼栋）、`respUnit`（责任单位）三个字段**报告都在用**（排序/分组/列），但**没有任何写入路径** | DV-22 字段清算 | 报告需要按楼栋分组、按重要等级排序时 | `Defect.copyWith` 已支持（2026-09-18）；需详情页表单 + 可能的批量录入 |
| **F5** | **详情页「待复核」状态位**：目前「仅保存，待复核」只是按钮文案，没有对应状态位 | DV-21④ | 需要统计/筛选"待复核"条目时 | `DefectStatus` 扩展或新增独立字段 + 列表筛选 |
| **F6** | **回复照片上传**：模型有 `replyPhotoPath`，详情页无上传入口，报告「整改后照片」永远空 | DV-21③ | 要求整改前后对比取证时 | 详情页回复表单加取图（复用 `pickPhotoRobust`）+ 落盘 + 报告已支持渲染 |
| **F7** | **验收页量尺校对接入项目级判定门槛**：现在用页面内 ±10mm/±5%，与拍照量尺页的 `MeasureThresholdStore` 不一致 | DV-29 | 与量尺规格一起处理时 | 属量尺模块，见 `MEASURE_*` 系列规格 |
| **F8** | **Web 端持久化** | DV-28（判定：Web 仅测试用，不管） | 需要把 Web 当真实工作端时 | `app_storage_web.dart` 文件层由内存改 IndexedDB / 服务端 |
| **F9** | **后端重建相关（批 3）**：项目隔离统一 `project_id`、id 统一 `client_uuid`、软删 `deleted_at`、`updateDefect` 健壮性、权限体系 | register §5 | 后端重建启动时 | 与 `BACKEND_ARCHITECTURE.md` 一起做；`Defect` 侧已预留 `sourceCaptureIdx` 等后端可直接映射的字段 |

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 首版：从 `DEVIATION_REGISTER` 的决策结果中分离出 9 项「要做但本次不做」的功能 |

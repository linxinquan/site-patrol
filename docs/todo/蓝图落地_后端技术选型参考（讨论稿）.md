# 蓝图落地 · 后端技术选型参考（讨论稿 · 过程记录）

> ⚠️ **本文是过程记录，不是结论。** 选型结论以同目录 **《蓝图落地_后端技术选型（定稿）.md》** 为准。
> 本文保留的是**当时怎么想出来的**（候选优缺点、横向对比、口径修订）。正文里凡涉
> 「宝塔 / PM2 / Docker Compose / 单机 ECS / 不碰 SSH / PostgreSQL 16」的论证，一律按**过程**理解 ——
> 这些前提**已被取代**（现为 **k8s**，由团队内运维成员执行）。
>
> **读者**：项目组
> **用途**：保留选型的**评估过程** —— 复盘"为什么不用某个方案"时查这里。
>
> 日期：2026-09-21
>
> **当时的已定项**（⚠️ **以定稿为准**）：
> - 语言 / 框架 = **Python 3.12 + FastAPI**（本文结论，见 §5）—— ✅ 仍然有效
> - 对象存储 = **阿里云 OSS**（华南1 深圳）—— ✅ 仍然有效
> - ~~服务器 = **阿里云现有 ECS node0** `47.106.123.210`~~ —— ⚠️ **已作废**：部署改 k8s，集群与 PG 实例由团队内运维成员提供
> - ~~数据库 = **PostgreSQL 16**~~ → **PostgreSQL**（不锁版本）
> - 不考虑预算
>
> **一个影响全文的前提**：**主要靠 AI 辅助开发**。
> 因此下面判据的权重跟"专业后端团队"不一样 —— 先说清标准，再看候选。

---

## 1. 按什么标准比（判据口径）

| #   | 判据                   | 为什么对我们重要                                                      |
| --- | ---------------------- | --------------------------------------------------------------------- |
| 1   | **AI 能多准地写它**    | 主要靠 AI 出代码 → 语言语料越厚、范式越标准，生成质量与自我纠错越可靠 |
| 2   | **出错时能搜到答案**   | 没人能凭经验排障，生态资料量直接决定"卡住多久"                        |
| 3   | **工具链能替你查错**   | 强类型 / schema 校验 / 迁移自动化 = AI 写完代码之后的质量底线         |
| 4   | **部署运维自己搞得定** | 「不碰 SSH」+ 与现有业务**共机**（不得影响现有服务）                  |
| 5   | **与已有资产的一致性** | 客户端 Dart · 现有 AI 网关 Node                                       |
| 6   | 性能 / 并发 / 内存     | **对本项目几乎无意义**：QPS 极低，瓶颈在 PG 与 OSS 外网流量           |

**一句话口径**：#1 ~ #4 是我们真正的痛点，#6 可以忽略。下面三个候选就按这个口径比。

---

## 2. 现状事实（实盘核对，2026-09-21）

### 2.1 这个后端要做什么（决定难度上限）

**CRUD + jsonb 透传 + 变更流（离线同步）+ 文件签名 URL + JWT + AI 转发与记账。**

全是"标准活"—— 没有一项是某种语言独有的能力，三选一都能做完。详细清单见
`BACKEND_ARCHITECTURE.md` §7（表结构）· §9（同步机制）· §13（接口清单）。

### 2.2 已有资产

| 资产         | 事实                                                                                                                                                             | 对选型的影响                                                                                                                      |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| **AI 网关**  | 仓库外 `gongdi/backend/`：Express 4，**5 条路由**（`/health` `/api/vision` `/api/chat` `/api/video/*`），**≈10 KB**，且是**多项目共用**（chat/video 属其他项目） | ① 体量极小 → **不构成"必须继续用 Node"的理由**（任何语言重写约 1–2 天）② **不应搬入新后端**，只需在它前面加鉴权/记账/缓存层（§6） |
| **客户端**   | 模型即契约：`lib/data/models/` 11 个域文件，`toJson/fromJson` **唯一决定落库形态**；`SyncMeta`（ULID 幂等键）已就位                                              | 服务端只需"按 schema 校验 + 原样落 jsonb" → **校验与迁移工具很关键**（判据 #3）                                                   |
| **部署路径** | 宝塔「Python 项目管理器」可管理 uvicorn/gunicorn 进程                                                                                                            | Python 需要在宝塔配置 Python 项目（见 §3.2），**本机已有 Python 运行时事实可用**                                                  |

---

## 3. 三个候选

### 3.1 Node 22 + TypeScript 5 + Express 5（+ Prisma + Zod）

|            |                                                                                                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **优点**   | ① **AI 语料最厚**，生成与纠错最可靠（判据 #1 #2）② **Prisma**：schema 即文档 + 迁移自动 diff + 类型生成（判据 #3）③ 宝塔「Node 项目管理器」**路径已在跑**（判据 #4）④ 与现有网关同栈，排障资料可复用 |
| **代价**   | ① 共机多一套 Node 运行时（但**本来就在跑**）② 常驻内存比 Go 高些 ③ 弱类型惯用法容易漏错 → 必须靠 `strict` + Zod 兜住                                                                                 |
| **对我们** | 摩擦很小的一条，但最终被 Python 在"schema 校验 + 自动文档"上的优势压过（见 §5）                                                                                                                       |

### 3.2 Python 3.12 + FastAPI（+ SQLAlchemy 2.0 / asyncpg + Pydantic）—— ✅ **已选**

|            |                                                                                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **优点**   | ① **Pydantic** 是做"AI 返回结构校验 + 半结构化解析"最顺手的工具（本项目明确要做这件事）② FastAPI 自动出 OpenAPI 文档，交接友好 ③ AI 语料同样很厚 ④ SQLAlchemy + asyncpg 成熟，jsonb 支持好 |
| **代价**   | ① 宝塔要装「Python 项目管理器」+ uvicorn/gunicorn 组合，**配置项比 Node 多**（判据 #4 偏弱）② 运行时类型安全弱于 TS / Go ③ 共机多一套 Python 运行时                                        |
| **对我们** | **已采用**。配置项多这件事是一次性的，换来的是 Pydantic 校验体验 + 自动 OpenAPI 文档这两条长期收益                                                                                          |

### 3.3 Go 1.23 + chi（或 gin）+ pgx

|            |                                                                                                                                                                                                                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **优点**   | ① **部署最干净**：编译成单个静态二进制 → 宝塔的**进程守护（Supervisor）管理器**守护任意可执行文件即可，**不装运行时、不要语言专用项目管理器**（判据 #4 最强）② 常驻内存最小（十几 MB）③ 编译期强类型（判据 #3）④ `json.RawMessage` 最贴合"服务端不解析 jsonb"的设计 ⑤ 性能/并发强（对本项目无意义） |
| **代价**   | ① **AI 语料明显少**，生成质量与纠错都吃亏；写法也更啰嗦（错误处理冗长、无 ORM 一等公民）② **没有 Prisma 等价物**：迁移用 `goose` / `golang-migrate`，索引等要手写 SQL ③ 出错时社区答案最少 ④ 学习与排障成本最高                                                                                     |
| **对我们** | 已排除：技术属性最"干净"，但**与我们的两条主要判据（#1 AI 能准确写、#2 搜得到答案）方向相反**                                                                                                                                                                                                        |

### 3.4 附：为什么不建议用 Dart 写后端

它唯一真正的吸引力很硬：客户端模型 `toJson/fromJson` **本身就是落库契约**，若后端也是 Dart，
可以**共享同一份模型**，契约漂移归零，而且 app 与服务端只剩一种语言。

但障碍是实的：① `models/defect.dart` 依赖 `package:flutter/material.dart`（不是纯 Dart），想共享得先抽包；
② 没有 Prisma 等价物，服务端生态小（shelf / dart_frog / Serverpod）；③ 宝塔没有 Dart 项目类型，要 PM2 自定义脚本 + AOT 编译。

> **一个更省的做法**：如果将来"客户端与服务端字段不一致"真的成为反复出现的痛点，
> 该做的是把 `lib/data/models/` 抽成**无 Flutter 依赖的纯 Dart package**（顺带可导出 JSON Schema 给后端做校验），
> **而不是换后端语言** —— 收益类似，成本低一个量级。

---

## 4. 横向对比

| #   | 判据                 | Node + TS                                | Python + FastAPI                                       | Go                                   |
| --- | -------------------- | ---------------------------------------- | ------------------------------------------------------ | ------------------------------------ |
| 1   | AI 能多准地写它      | 优（语料最厚）                           | **已选**                                               | 中（语料少、写法啰嗦）               |
| 2   | 出错时搜得到答案     | 优                                       | **已选**                                               | 中                                   |
| 3   | 工具链替你查错       | 优（TS strict + Zod + Prisma 迁移）      | 良（Pydantic + SQLAlchemy + Alembic）                  | 中（编译期强类型，但迁移要手写 SQL） |
| 4   | 部署运维自己搞得定   | 优（宝塔 Node 路径已在跑）               | 良（宝塔 Python 项目管理器 + uvicorn/gunicorn，一次性配置） | 优（单二进制 + 进程守护）            |
| 5   | 与已有资产一致性     | 优（网关同栈；客户端是强类型的 Dart）    | 良（与已有栈不同族，但客户端契约靠 schema 校验对齐）   | 弱（全新栈）                         |
| 6   | 性能 / 并发 / 内存   | 够用                                     | 够用                                                   | 强（**本项目无意义**）               |
| —   | 综合（按我们的口径） | 次选                                     | ✅ **采用**                                            | 已排除（学习/排障成本最高）          |

---

## 5. 结论：Python 3.12 + FastAPI

**决定：Python 3.12 + FastAPI + SQLAlchemy 2.0（asyncpg 驱动）+ Pydantic v2 + Alembic。**

理由按判据排序：

1. **#3 工具链替你查错（决定性）**：`Pydantic v2` 是"AI 返回结构校验 + 半结构化解析"最顺手的工具，本项目的 AI 结果落库、jsonb 字段校验都能用同一套模型表达；FastAPI 的请求/响应模型即 schema，**AI 写错了会在启动或请求时直接报错**，而不是静默降级。这一条正是"客户端模型即契约"形态最需要的。
2. **#1 #2**：AI 语料同样很厚，出问题搜得到答案，与 Node 同级。
3. **#5 交接友好**：FastAPI 自动生成 OpenAPI 文档，客户端（Dart）侧可直接据此生成/校对请求模型，契约漂移有据可查。
4. **#4**：宝塔「Python 项目管理器」+ `uvicorn`（或 `gunicorn -k uvicorn.workers.UvicornWorker`）是一次性配置成本，配置完之后与 Node 路径同样省心。

代价（共机多一套 Python 运行时、运行时类型安全弱于 TS/Go）**接受**：运行时类型安全的缺口由
`mypy`（CI）+ Pydantic 边界校验共同兜住（见 §8）。

---

## 6. 与语言无关、可以先推进的部分

**结论：下面这些与语言选择解耦，现在就可以推进。**

| 项                                                | 状态                                                                                                                 |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 数据模型 / 表结构（`BACKEND_ARCHITECTURE.md` §7） | 已定                                                                                                                 |
| 同步协议（seq 变更流 + 幂等，§9）                 | 已定，与语言无关                                                                                                     |
| 接口清单（§13）                                   | 已定，与语言无关                                                                                                     |
| 对象存储 / 服务器                                 | 已定（OSS 深圳 · node0）                                                                                             |
| **AI 网关的定位**                                 | **决定：保留原位**（多项目共用），新后端只在它前面加「鉴权 / 记账 / 缓存 / 返回校验」一层；**不搬入**                |
| **Redis**                                         | **决定：先不装**。会话吊销 / 限流 / AI 缓存 / 任务队列在初期都能用 PG 顶掉，少一个共机组件；有压力再加，接口层不用改 |
| 部署方式                                          | 宝塔「Python 项目管理器」+ uvicorn/gunicorn 守护。硬约束是"不碰 SSH + 不影响共机业务"                                |

---

## 7. 数据库：PostgreSQL（已定，与语言无关）

> 这条**不是偏好问题** —— 选错了会让离线同步**静默丢数据**。

### 7.1 数据形态

以**关系模型**为主（项目 → 图纸 → 缺陷 → 事件 → 回复 → 成员权限），
但有**大量半结构化字段**（客户端 `toJson()` 原样落库，服务端不解析几何）：

```
measure_sessions.items / calib        room_scans.walls / checks
patrol_records.track / checkins       projects.parties / milestones / measure_thresholds
drawings.hotspots                     captures.ai_result / confirmed_result
defects.tags / photos                 memberships.permissions
```

→ 需要一个**既能建外键关系、又能装 JSON 文档**的库。这一条直接排除 MongoDB（关联查询弱、无外键、事务能力受限）。

### 7.2 关键机制：同步变更流的序号分配

离线同步靠一张变更流表 + 游标：

```sql
changes(seq bigserial PK, project_id, entity, entity_id, op, changed_at)
-- 客户端只记一个游标，按 seq > cursor 拉增量
```

**陷阱**：`bigserial` 的**分配**严格递增，但**可见性由提交时间决定**。两个事务可能"先分配、后提交"：

```
T1  事务 A  nextval → seq = 100（未提交）
T2  事务 B  nextval → seq = 101，随后提交
T4  客户端拉取，读到 max(seq) = 101 → 游标推到 101
T5  事务 A 提交，seq = 100 才可见
T6  客户端从 101 继续拉 → 100 永远拉不到（静默丢一条变更，无任何报错）
```

**PostgreSQL 的解法（1 行）**：在写 `changes` 的同一事务开头取**事务级咨询锁**，让序号分配被串行化，
于是"分配顺序 ≡ 提交顺序"，`seq > cursor` 恢复正确：

```sql
SELECT pg_advisory_xact_lock(981001);   -- 事务结束自动释放，不会泄漏
INSERT INTO changes(project_id, entity, entity_id, op) VALUES (...) RETURNING seq;
```

代价：变更流写入被串行化。本项目写入 QPS 极低，**代价可忽略**。

**MySQL 为什么不行**：只有**会话级** `GET_LOCK()`，必须显式 `RELEASE_LOCK`；连接池下极易泄漏或误释放，
**没有可靠的写法**。这是"必须 PG"的最硬理由。

### 7.3 其它对本项目有实际影响的差异

| 项                           | PostgreSQL                                           | MySQL 8                                         | 我们用在哪                                  |
| ---------------------------- | ---------------------------------------------------- | ----------------------------------------------- | ------------------------------------------- |
| **数组类型 `text[]`**        | ✅ 原生 + GIN 索引                                   | ❌ 无原生数组                                   | `memberships.disciplines`（成员专业维度）   |
| **JSONB 一等类型 + GIN**     | ✅ 可索引、可 `@>` 包含判断                          | 🟠 JSON 需借生成列建索引，操作符能力弱          | `items` / `walls` / `track` / `ai_result` … |
| **部分索引**                 | ✅ `CREATE INDEX … WHERE deleted_at IS NULL`         | ❌ 无                                           | 全表软删，能显著压缩索引体积                |
| **`ON CONFLICT DO NOTHING`** | ✅ 幂等 upsert 的天然表达                            | 🟠 `INSERT IGNORE` 会吞掉所有错误（含数据截断） | `client_id` 幂等重发                        |
| **`pg_trgm` 模糊召回**       | ✅ `similarity()` 直接排序                           | 🟠 相似度排序弱                                 | 规范条款 / 历史缺陷召回                     |
| `timestamptz`                | ✅ 时区语义正确                                      | 🟠 `TIMESTAMP` 有时区与 2038 问题               | 全部时间列                                  |
| 宝塔运维                     | ✅ 软件商店有 PG 管理器；备份用 `pg_dump`            | ✅ **宝塔最强项**（备份/phpMyAdmin）            | 日常运维                                    |

> **学习成本要诚实说**：PG 与 MySQL 在 CRUD 层面差异很小，真正要适应的是三件事 ——
> ① `schema` 与用户/权限模型 ② JSONB 查询语法（`->` / `->>` / `@>`）③ 标识符默认折叠为小写（建表用小写蛇形即可避开）。
> **这是 1–2 天的适应成本，不是项目风险**；而"静默丢数据"是项目风险。

**结论：PostgreSQL 16**，扩展 `pgcrypto`（`gen_random_uuid`）+ `pg_trgm`；PostGIS 暂不装。

---

## 8. 一起做对的 5 件事

| #   | 事项                           | 说明                                                                                                                                                     |
| --- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **验证发生在服务端**           | 客户端模型是契约，服务端必须用 **Pydantic 模型**校验入参（并校验 AI 返回、失败自动重试一次），不静默降级、不静默 mock                                     |
| 2   | **迁移从第一天就有**           | **Alembic** 管理迁移，每个 migration 向前兼容（新增列可空、不删旧列）；三处要手写 SQL：部分索引 · GIN（数组/jsonb）· 咨询锁                                |
| 3   | **密码哈希用零原生依赖的实现** | 共机 + 不碰 SSH → 别引入需要编译的东西：Python 用标准库 **`hashlib.scrypt`**                                                                             |
| 4   | **结构化日志 + `/health`**     | 没人能上机器看日志，日志必须是可检索的 JSON（`structlog` 或 `logging` + JSON formatter）；`/health` 聚合 DB / OSS / AI 上游                              |
| 5   | **组件分阶段加**               | 先只上 PG；Redis / 后台 Worker 有真实压力再加（接口层不用改）                                                                                             |

---

## 9. 选型后待明确的问题

1. **后端长期由谁维护？**（当前由我们两个 + AI，Pydantic/FastAPI 上手成本低，维护无阻碍）
2. **AI 网关**：已决定留原地（多项目共用），新后端在其前面加鉴权/记账/缓存/返回校验层。
3. **部署落地细节**：宝塔如何安装「Python 项目管理器」、Python 版本选择（3.12）、
   `uvicorn`/`gunicorn` 工作进程数、虚拟环境（`venv`）位置、`systemd`/宝塔守护方式的确认。
4. **依赖管理**：建议 `uv` + `pyproject.toml`（锁文件 `uv.lock`），避免共机上编译依赖。
5. **PG 连接**：`asyncpg` + SQLAlchemy 2.0 async session，连接池大小（QPS 极低，池可很小）。

> 语言与数据库已定，§6 里"与语言无关"的部分（表结构 / 同步协议 / 接口）
> **现在就可以动手**，不会因为后续细节调整而返工。

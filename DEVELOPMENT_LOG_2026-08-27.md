# 开发日志 2026-08-27（周四）

## 一、今日完成

### 1. F 盘发现大铲湾 DY04 完整资料
之前以为数据在 G 盘（断开），实际 `F:\建筑验收工具\大铲湾DY04_资料\` 含完整 7 栋施工图
（DWG + PDF + DXF）。用 Python 检查所有 PDF 物理页面尺寸 + DXF 实体构成。

### 2. B01 精确校准（核心突破）

**关键发现**：用户提供的 B01 DXF（`建施_AW-7-B01_V1.0_地下一层顶板组合平面图.dxf`）
虽经 ODA 转换，但**完整几何保留在块定义中**：
- 布局 `7栋组合图`：标题栏标注 **1:250**
- 模型空间 INSERT `AD-P-B1D` @ (0, 1240137.5)
- 块 `AD-P-B1D`：453 个 DIMENSION（真实模型坐标）
- 块 `AD-Dote-BF`：**轴网层** `A-Axis`（51 竖 + 32 横）+ `A-Axis-GRID`（9 竖 + 29 横）
  - 轴距 **8400mm**（DIMENSION 测量值 160 次）

**校准推导**（暗列/暗行 + 轴网线匹配）：
- **scale=66.222 mm/px**（= 1192×250/4500 = PDF 物理宽×1:250 / 底图像素宽）
- **c=19500**：竖线 7/9 精确命中底图暗列（X 像素 1354/1735/1862/2369/2496/3004/3511）
- **f=1430000**：横线 21/21 精确命中底图暗行（Y 像素 2867/2740/.../76）
- 底图右侧未画完整轴网（X>302400 超出打印窗口），属正常

**数据文件更新**：
- `assets/axis_data/dy04_7_B01.json` 重新生成：**261 个交点**（原 174 个来自错误 DXF，
  X 坐标偏移严重，导致 B01 校准不准确）
- `lib/core/di/providers.dart` builtinCalibrationFor B01：
  a=24.88 → **66.222**, c=291881.8 → **19500**, f=1300500 → **1430000**

### 3. D01 / D03 / D04 精确校准种子（基于 PDF 物理 + 打印比例）

| 图纸 | PDF 物理 | 打印比例 | a mm/px | c | d mm/px | f |
|---|---|---|---|---|---|---|
| D01 | 1265.1×596.9 mm | 1:150 | 79.17 | -172 | -79.02 | 89535 |
| D03 | 843.8×596.9 mm | 1:100 | 35.16 | 0 | -35.15 | 59690 |
| D04 | 843.8×596.9 mm | 1:100 | 35.16 | 0 | -35.15 | 59690 |
| B01 | 1192.0×843.8 mm | 1:250 | 66.22 | 19500 | -66.22 | 1430000 |
| B05 | 1489.1×843.8 mm | (视口真实校准) | 0.331 | -359.3 | -0.331 | 852.4 |

**关键证据**：
- D01 RANSAC 1D 投票 7/7 inliers 与 axis_data JSON 对齐（a=79.17, c=-172）
- B01 暗列/暗行精确匹配（见上）
- 所有图 a = PDF物理宽 × 打印比例 / 底图像素宽

### 4. 单元测试新增/更新
- `test/builtin_seed_test.dart`：5 个测试全过（4 张图 a/d 精度 + 未知图 null）
- `test/axis_auto_calibration_test.dart`：8 个测试全过（detectAxisLines v2 + 轴网匹配）
- 总计 13 个测试全部通过

## 二、未完成 / 遗留事项

1. **detectAxisLines 仍被标高线干扰** — D01 底图 v2 检出 10 横线，其中含标高线/剖面线，
   需进一步区分"轴网横线"与"标高/剖面线"（可结合 TEXT 实体分布或水平虚线特征）。
2. **D01 RANSAC Y 方向** — 被标高线干扰，改用 PDF 物理推算 f=89535（已足够精确）。
3. **其他图纸**（K01/E01/J01/F01 等详图）尚未检查校准 — 它们可能也是块内几何，
   可用同样方法（PDF 物理 + 打印比例 + 暗线匹配）精修。

## 三、技术要点

### 1. 天正 DXF 几何在块定义中
天正图经 ODA 转换后，模型空间实体稀少（B01 只剩 7 HATCH + 8 LWPOLYLINE + 1 INSERT），
**完整几何都在 INSERT 引用的块定义中**。用 ezdxf 需：
```python
msp = doc.modelspace()
for e in msp.query("INSERT"):
    blk = doc.blocks.get(e.dxf.name)  # 进入块
```
块内坐标 + INSERT 偏移 = 模型空间坐标。

### 2. 轴网层识别
- `AD-Dote-BF$0$A-Axis`：完整轴线（51 竖 + 32 横，间距 8400mm）
- `AD-Dote-BF$0$A-Axis-GRID`：主轴网（9 竖 + 29 横）
- `AD-P-B1D$0$N-GRID建筑轴线`：16 条 LINE
- `AD-P-B1D$0$PUB_DIM`：423 个 DIMENSION（主尺寸标注）

### 3. 校准匹配方法（B01 成功）
1. 从 DXF 轴网层提取竖线 X / 横线 Y（模型坐标）
2. 底图降采样（min-pooling）统计暗列/暗行峰值
3. 用「暗列间距中位数」估算 scale：scale = 轴距(8400) / 暗列间距(128px) = 65.6
4. 与 PDF 物理推算交叉验证：scale = PDF宽×打印比例/底图宽 = 66.222
5. 固定 scale，搜索 c/f 使轴网线投影命中暗列/暗行最多

### 4. a/c/d/f 符号约定
仿射 wx = a*px + c, wy = d*py + f，d 是负的（像素 Y 向下，CAD Y 向上）。
- a > 0：模型 mm / 像素
- d < 0：模型 mm / 像素（Y 翻转）
- c = X_render_min（底图最左像素对应的模型 X）
- f = Y_render_max（底图最上像素对应的 CAD Y 最大值）

## 四、已更新的文件
- `lib/core/di/providers.dart`：B01 种子精修（a=66.222, c=19500, f=1430000）
- `assets/axis_data/dy04_7_B01.json`：重新生成（261 交点，正确坐标）
- `test/builtin_seed_test.dart`：B01 期望值更新
- `DEVELOPMENT_LOG_2026-08-27.md`：本日志

## 五、积分/额度消耗
- 全部本地工具改进（无 API 额度）

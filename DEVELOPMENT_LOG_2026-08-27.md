# 开发日志 2026-08-27（周四）

## 一、今日完成

### 1. F 盘发现大铲湾 DY04 完整资料
之前以为数据在 G 盘（断开），实际 `F:\建筑验收工具\大铲湾DY04_资料\` 含完整 7 栋施工图
（DWG + PDF + DXF）。用 Python 检查所有 PDF 物理页面尺寸 + DXF 实体构成。

### 2. D01 / D03 / D04 精确校准种子（基于 PDF 物理 + 打印比例 + RANSAC 1D 投票）

| 图纸 | PDF 物理 | 打印比例 | a mm/px | c | d mm/px | f |
|---|---|---|---|---|---|---|
| D01 | 1265.1×596.9 mm | 1:150 | 79.17 | -172 | -79.02 | 89535 |
| D03 | 843.8×596.9 mm | 1:100 | 35.16 | 0 | -35.15 | 59690 |
| D04 | 843.8×596.9 mm | 1:100 | 35.16 | 0 | -35.15 | 59690 |
| B01 | 1192.0×843.8 mm | 1:100 | 24.88 | 291881.8 | -24.88 | 1300500.0 |
| B05 | 1489.1×843.8 mm | (视口真实校准) | 0.331 | -359.3 | -0.331 | 852.4 |

**关键证据**：
- D01 RANSAC 1D 投票在底图 2400px 上 7/7 inliers 与 axis_data JSON 7 个 X 对齐，
  给出 a=79.17, c=-172（X 方向精度 < 0.1mm）
- D03/D04 PDF 物理 843.8mm + 1:100 → a=35.16 与之前估算 37.125 接近但更准（误差从 6% → 0）
- B01 1:100 打印窗口 1120×795mm → a=24.88（X 方向经验值，< 1% 误差）

### 3. B01 Y 方向精度精修（已有数据基础上保留）
- B01 DXF 经 ODA 转换后只剩 ACAD_PROXY_OBJECT（天正自定义对象丢失），无 DIMENSION
- B01 之前 f=1300500（圆点匹配近似）→ 保留
- 真实精度需 G 盘/天正版 B01 T3 DXF 进一步精修

### 4. D01 / D03 / D04 内置种子更新（lib/core/di/providers.dart）
之前估算 a=37.125（误差 6%），更新为 PDF 物理 + 打印比例精确推算值（误差 < 0.1%）。

### 5. 单元测试新增（test/builtin_seed_test.dart）
5 个测试全过：验证 4 张图 a/d 与预期 PDF 物理 + 比例吻合（精度 0.01 mm/px），
验证未知图返回 null。

### 6. 检测 v2 + RANSAC 1D 投票（昨日）复用
- D01 RANSAC X 方向 7/7 inliers，验证 a=79.17, c=-172
- D01 RANSAC Y 方向失败（底图"横线"含标高线/剖面线干扰），
  改用 PDF 物理推算 d, f

## 二、未完成 / 遗留事项

1. **B01 Y 方向精度** — 需 G 盘 / 天正版 B01 T3 DXF（含 2583 个 DIMENSION 标注），
   才能用 DIMENSION 端点 + PDF 渲染像素配准，< 1mm 精度。
2. **detectAxisLines 仍被标高线干扰** — D01 底图 v2 检出 10 横线，其中含标高线/剖面线，
   需进一步区分"轴网横线"与"标高/剖面线"（可结合 TEXT 实体分布或水平虚线特征）。
3. **D03/D04 底图渲染的 X 范围未知** — axis_data JSON X 范围 [-41904, 39546] 跨度 81450mm，
   略小于 PDF 物理 1:100 = 84380mm，说明底图渲染窗口略小于完整 PDF。
   实际 X 渲染最小值可能 ≠ 0。但因 Y 方向已被 PDF 物理约束，X 方向对角点打点量尺误差在 ±1mm 内可接受。

## 三、技术要点

### 1. PDF 物理页面 + 打印比例 = a/d 精确值
CAD 打印时记录 paper size + scale，a = paper_w_mm × scale / img_w_px。
DXF 布局中的 paper_size 是 DWG 默认设置，实际 PDF 可能用不同打印参数，
**必须直接测量 PDF 物理尺寸（PyMuPDF page.rect）**。

### 2. RANSAC 1D 投票
- 对 (cad_v, vX)，枚举所有 cad 间距 / px 间距比值 = scale 候选
- 投票取最密 scale，再对每个 (scale) 试各种 offset，inlier 最多的胜出
- D01 X 方向成功（轴网竖线贯穿全图，cad_v 不被标高线干扰）
- D01 Y 方向失败（标高线、剖面线都是横线，hY 含大量非轴网横线）
- **改进方向**：先识别"长线 vs 短线"，过滤标高/剖面线

### 3. a/d/c/f 符号约定
仿射 wx = a*px + c, wy = d*py + f，d 是负的（像素 Y 向下，CAD Y 向上）。
- a > 0：模型 mm / 像素
- d < 0：模型 mm / 像素（Y 翻转）
- c = X_render_min（底图最左像素对应的模型 X）
- f = Y_render_max（底图最上像素对应的 CAD Y 最大值）

## 四、已更新的文件
- `lib/core/di/providers.dart` builtinCalibrationFor：D01/D03/D04 种子更新
- `test/builtin_seed_test.dart`：新增 5 个种子精度测试

## 五、积分/额度消耗
- 全部本地工具改进（无 API 额度）

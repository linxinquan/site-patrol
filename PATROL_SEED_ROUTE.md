# 巡场：用户自规划路线 + 全局推荐路线（交付 CodeBuddy）

> 在 `PATROL_OPTIMIZE.md`（④路线编辑器）与 `PATROL_ZOOM_WALL.md`（缩放+穿墙检测）基础上叠加。
> 三件事：①编辑器增删点交互补全 ②默认"全局巡场推荐路线"（不穿墙、符合工程习惯）③推荐路线作为模板可复制编辑。

---

## 1. 编辑器交互补全（在 ④ 基础上增强）

| 操作 | 行为 | 状态 |
|---|---|---|
| 单击图纸空白处 | 追加路点 | 已有 |
| 拖动点 | 移动 | 已有 |
| **单击已有点** | **选中高亮**，点旁浮现小操作钮：`删除` / `设为检查点` / `取消检查点` | 新增（比长按更易发现） |
| 长按点 | 删除 | 已有（保留） |
| 双击点 | 切换检查点 | 已有（保留） |
| 撤销 / 清空 | 移除最后点 / 清空 | 已有 |
| **校验穿墙** | 一键对整条路线跑墙线检测（`PATROL_ZOOM_WALL.md` P1-2 的 `routeSegmentHitsWall`），弹出结果："无穿墙 ✓" 或 "N 段穿墙"，穿墙段红色高亮 | 新增 |
| **复制路线** | 把当前路线另存为新计划（"xx-副本"），推荐路线模板复制后可自由增删点 | 新增 |
| 保存 | points≥2 才可保存；有穿墙段时弹确认框（允许保存） | 已有 |

选点小操作钮实现提示：选中点在 `_selectedIdx` 状态，绘制时给该点描边高亮；按钮用 Positioned 浮层或底部工具条（选中时显示"删除所选点/设为检查点"两个按钮即可，最省）。

## 2. 推荐路线（全局巡场）设计规范

**工程习惯原则（写进产品，作为检查点配置指引，也用于演示话术）**：

| 原则 | 说明 |
|---|---|
| 闭环 | 起点=终点（主入口出发、回主入口），一条路线覆盖全层 |
| 只走公共走道 | 沿走廊/疏散通道走，不穿房间；机房/设备房作为检查点**进入后原路返回** |
| 顺疏散方向 | 路线方向优先沿疏散指示（与消防巡检习惯一致） |
| 检查点八类 | ①主入口 ②楼梯间（每部）③电梯厅 ④扶梯 ⑤坡道口 ⑥设备机房/水泵房/变配电（本层如有）⑦防火分区门 ⑧消火栓/管井集中处 |
| 点密度 | 走廊拐角必须有路点；直线段每 15~25m 一个点（保持视线连续） |
| 不穿墙硬约束 | 保存推荐路线前必须跑"校验穿墙"通过（0 穿墙段） |

## 3. B05 全局巡场推荐路线骨架

> 基于图纸文件信息（A座 LT01~LT03 楼梯、B座 QLT2-4/LT04/LT05 楼梯、自动扶梯 FT-4EC3-01、电梯、夹层、坡道）设计。**坐标为占位值**，必须按 §4 流程在编辑器里校准后才可标记为推荐路线（本模型无法读取图片，不能直接给出走廊精确坐标——工具自校验兜底）。

路线骨架（15 点，★=检查点）：

```
主入口（★①）→ 沿 A 座主走廊 → A 座核心筒：LT01 楼梯间（★②）
→ 电梯厅（★③）→ LT02/LT03 楼梯（★④）→ 自动扶梯 FT-4EC3-01（★⑤）
→ A/B 座连廊 → B 座核心筒：B 座楼梯（★⑥）→ B 座电梯厅（★⑦）
→ B 座走廊环绕 → 坡道口（★⑧）→ 设备机房区（★⑨）
→ 防火分区门（★⑩）→ 沿主走廊回主入口（闭环）
```

占位坐标表（相对 0-100，dy04_7_B05，4500×2551）：

```
点01 主入口          (50, 90) ★
点02 走廊拐角        (40, 90)
点03 A座LT01楼梯间   (40, 70) ★
点04 A座电梯厅       (45, 70) ★
点05 A座LT02/LT03    (50, 70) ★
点06 扶梯FT-4EC3-01  (58, 70) ★
点07 A/B连廊         (62, 65)
点08 B座楼梯         (68, 65) ★
点09 B座电梯厅       (72, 65) ★
点10 B座走廊拐角     (75, 55)
点11 坡道口          (75, 40) ★
点12 设备机房区      (68, 40) ★
点13 走廊拐角        (60, 35)
点14 防火分区门      (50, 35) ★
点15 回主入口        (50, 90)（与点01闭环）
```

> ⚠️ 这些坐标是**演示占位**，不是真实走廊位置。真实推荐路线的产生流程见 §4——这是有意的设计：**"推荐路线=模板骨架+工具校准+穿墙自校验"**，保证交付的路线真实不穿墙。

## 4. 推荐路线验收流程（必须走完才算"推荐路线合格"）

1. 跑通 `PATROL_ZOOM_WALL.md` P1（墙线导出 + 检测）
2. 编辑器打开推荐路线 → **放大到走廊级** → 按 §2 原则把占位点逐个拖到真实走廊中线（拐角加点、楼梯/电梯厅/机房门口设检查点）
3. 点"**校验穿墙**" → 目标 **0 穿墙段**；有红色段就补点/移点消除
4. 检查里程：巡场页里程 chip 显示图纸实算 km（B05 已校准），与常识核对（整层闭环几百米量级合理）
5. 保存 → 路线带"推荐"徽标置顶

## 5. 数据模型小改

`lib/data/models.dart` 的 `PatrolPlan` 加字段：

```dart
final bool isRecommended; // 默认 false；推荐路线模板 true
// copyWith / toJson / fromJson 同步（fromJson: m['isRecommended'] == true）
```

`seedPatrolPlans` 的 7栋计划改为两条：
- `id:'dy7_recommended'`，`isRecommended:true`，points 用 §3 占位坐标（名字"B1 全局巡场（推荐）"）
- 用户通过"复制路线"生成自己的计划后编辑保存

列表/选择页：推荐计划置顶 + "推荐"徽标（badge，用现有 status_badge 风格）。

## 6. 验收清单

- [ ] 编辑器：单击加点/拖动/单击选中→删除按钮/双击切检查点/撤销/清空/保存 全部可用
- [ ] 编辑器：点"校验穿墙"→ 占位路线显示 N 段红色（证明检测生效）→ 按 §4 调整后 → 0 段绿色通过
- [ ] 复制路线 → 新计划出现"副本"，增删点自由
- [ ] 巡场页：默认打开推荐路线（带徽标），路线沿走廊，里程为图纸实算值
- [ ] 占位路线调整后的最终推荐路线坐标**回写**进 `seedPatrolPlans`（替换占位值），作为正式默认
- [ ] `flutter analyze` + Web 构建通过

## 7. 演示话术

> "系统默认给一条全局巡场推荐路线：沿疏散通道闭环走一圈，楼梯、电梯、扶梯、机房、坡道、防火门这些关键部位自动设为检查点。路线是可以自己改的——加点、删点、拖动都行，改完点'校验穿墙'，系统告诉你路线有没有穿墙。**推荐路线是我们按工程习惯做的模板，你复制一份就能改成自己项目的。**"

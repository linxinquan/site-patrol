开始本次开发任务。工作目录 `F:\建筑验收工具\site-patrol`。

【背景】你接手一个迭代中的 Flutter 工程（蓝图落地）。重要：**你之前可能换过登录账号，没有本会话的历史记忆**——所有"哪些已完成、文档在哪"都以本机文件为准，不要凭记忆猜测或重写已有功能。

【动手前必读（按顺序）】
1. `SESSION_CONTEXT.md` —— 项目记忆压缩包（团队/产品/技术/红线）
2. `TASK_DASHBOARD.md` —— 任务看板（文档地图 + 各任务状态 + 三段式排期）
3. 当前任务文档（见下方"本次任务"指向的文件）

【总纪律（每轮通用，不可违背）】
1. 改动前先读最新文件；`lib/data/models.dart` 多线共用——**只追加不覆盖**，fromJson 新字段给默认值，旧数据不崩
2. 报告/统计改动必须 HTML/PDF/DOCX/XLSX 四端同步
3. 不新增第三方依赖（除非任务文档明确允许）
4. 本机是 Windows：**不能编译 iOS**；验证用 `flutter analyze`（若 analysis server 崩 exit 255，改用 `dart analyze <改动目录>`）+ `flutter test` + `flutter build web --release`
5. [原生-待真机] 的代码写好即可，**不伪造验证结果**，交付时列"未验证清单"
6. 每项完成输出：改动文件清单 + 验证结果 + 验收自查
7. 超出任务范围的改动：先停下说明，不要顺手做

【本次任务】
执行量房功能 · 段1（数据模型+存储+几何引擎+测试）。
细节与代码全部在 `PASTE_ROOM_P1.md`（直接照做）+ `ROOM_MEASURE_IMPL_DETAIL.md` §1~§4（代码源）。
只做段1（§1~§4），**禁止**实现 §5 及以后的页面/路由/provider（段2）。
完成后按 `PASTE_ROOM_P1.md` 第五步格式输出。

【遇到问题】
1. 编译/测试报错：先自己读报错定位修复，修不了再报告（给出错行+你的分析）
2. 文档与代码冲突：以**最新代码**为准，报告中说明差异
3. 本机环境限制：如实报告"无法验证项"，不要假装通过
